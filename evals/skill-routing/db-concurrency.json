{
  "skill": "db-concurrency",
  "top_k": 3,
  "positive": [
    "my upsert sometimes creates two rows when the webhook retries",
    "designing an idempotency key for the payment queue handler so retries don't double-charge",
    "two workers grab the same job and both process it, what kind of lock do I need?",
    "I have a check then act flow: read the row, and if status is pending, transition the state machine. Can this race?",
    "when two people click save at the same time we end up with duplicate records",
    "writing a crash-recovery sweep that re-enqueues stuck jobs, worried it collides with a live worker"
  ],
  "negative": [
    {
      "prompt": "this endpoint is slow, I suspect an N+1 query and probably need an index or some caching",
      "owner": "performance"
    },
    {
      "prompt": "my migration adding a unique partial index fails on existing data in the table",
      "owner": "postgres-migrations"
    },
    "help me center a div vertically"
  ]
}
